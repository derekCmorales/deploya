---
description: Rol Derek — motor, tools, compose, adapters
globs: "**/construccion/**,**/orquestacion/**,**/enrutamiento/**,**/herramientas/**,**/adapters/**,**/compose*,**/docker-compose*,**/Dockerfile*"
alwaysApply: false
---

# Derek — M4 M5 M6 y capa herramientas M8

Dueño: @derekCmorales. Kit: `docs/roles/derek-motor-despliegue/`.

Alcance núcleo v4.1 ([docs/alcance.md](../../docs/alcance.md)): cola BullMQ, `docker build` con el `Dockerfile` del cliente, artefacto versionado, contenedor con límites del plan, subdominio con HTTPS y conmutación sin corte. Redesplegar reconstruye; reversión sin reconstruir y M8 están fuera de alcance (M8 queda como stub).

Puertos: `ContenedorPuerto`, `EnrutamientoPuerto`, `VerificacionEntornoPuerto`. Specs: `motor-construccion`, `orquestacion`, `enrutamiento`, `herramientas`. Commits `feat(m4):` / `feat(m5):` / `feat(m6):` / `feat(m8):`.
