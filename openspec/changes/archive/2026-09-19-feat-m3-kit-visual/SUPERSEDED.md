# Reemplazado — no se sincronizó al spec

Este change instaló el primer kit visual (monocromático, oscuro por defecto, `docs/kit-visual.md`). Lo reemplazó el **design system Deploya v4.1** (WEB-01): claro por defecto, acento Señal, fuente madre en [docs/diseno/](../../../../docs/diseno/README.md).

Se archiva como registro histórico. Sus deltas **no** se fusionaron a `openspec/specs/proyectos` ni `observabilidad` porque contradicen el sistema vigente. No lo uses como referencia de UI.
